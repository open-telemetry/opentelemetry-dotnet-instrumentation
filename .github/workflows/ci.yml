name: ci

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        machine: [ windows-2022, ubuntu-20.04, macos-10.15 ]
    runs-on: ${{ matrix.machine }}
    steps:
      - uses: actions/checkout@v3.0.2
      - uses: actions/setup-dotnet@v2.1.0
        with:
          dotnet-version: | 
            3.1.x
            6.0.x
      - name: Start MongoDB service
        if: ${{ runner.os == 'macOS' }}
        run: brew services start mongodb-community
      - name: Start SQL Server
        if: ${{ runner.os != 'Linux' }}
        run: docker run -e "ACCEPT_EULA=Y" -e "MSSQL_SA_PASSWORD=@someThingComplicated1234" -p 1433:1433 -d mcr.microsoft.com/mssql/server:2019-CU15-ubuntu-20.04
      - run: ./build.cmd Workflow
      - name: Upload logs
        uses: actions/upload-artifact@v3.0.0
        if: always()
        with:
          name: logs-${{ matrix.machine }}
          path: build_data/profiler-logs/
      - name: Upload binaries
        uses: actions/upload-artifact@v3.0.0
        if: always()
        with:
          name: bin-${{ matrix.machine }}
          path: bin/tracer-home
