imports:
    - /var/opt/crank/variables.yml

profiles:

  windows:
    variables:
      serverAddress: "{{ windowsIp }}"
    jobs:
      application:
        endpoints:
          - "{{ windowsEndpoint }}"
        environmentVariables:
          COR_PROFILER: "{918728DD-259F-4A6A-AC2B-B85E1B658318}"
          COR_PROFILER_PATH: "{{ windowsProfilerPath }}\\{{ commit_hash }}\\tracer-home-win\\win-x64\\OpenTelemetry.AutoInstrumentation.ClrProfiler.Native.dll"
          CORECLR_PROFILER: "{918728DD-259F-4A6A-AC2B-B85E1B658318}"
          CORECLR_PROFILER_PATH: "{{ windowsProfilerPath }}\\{{ commit_hash }}\\tracer-home-win\\win-x64\\OpenTelemetry.AutoInstrumentation.ClrProfiler.Native.dll"
          OTEL_DOTNET_TRACER_HOME: "{{ windowsProfilerPath }}\\{{ commit_hash }}\\tracer-home-win"
          OTEL_INTEGRATIONS: "{{ windowsProfilerPath }}\\{{ commit_hash }}\\tracer-home-win\\integrations.json"
          OTEL_AGENT_HOST: "{{ controllerIp }}"
          OTEL_TRACE_LOGGING_RATE: 6
          OTEL_TRACE_DEBUG: 0
        options:
          requiredOperatingSystem: windows
          requiredArchitecture: x64
          buildFiles:
          - "../../tracer-home-win/**;{{ windowsProfilerPath }}\\{{ commit_hash }}\\tracer-home-win"
      load:
        endpoints:
          - http://localhost:5010

  linux:
    variables:
      serverAddress: "{{ linuxIp }}"
    jobs:
      application:
        endpoints:
          - "{{ linuxEndpoint }}"
        environmentVariables:
          COR_PROFILER: "{918728DD-259F-4A6A-AC2B-B85E1B658318}"
          COR_PROFILER_PATH: "{{ linuxProfilerPath }}/{{ commit_hash }}/tracer-home-linux/OpenTelemetry.AutoInstrumentation.ClrProfiler.Native.so"
          CORECLR_PROFILER: "{918728DD-259F-4A6A-AC2B-B85E1B658318}"
          CORECLR_PROFILER_PATH: "{{ linuxProfilerPath }}/{{ commit_hash }}/tracer-home-linux/OpenTelemetry.AutoInstrumentation.ClrProfiler.Native.so"
          OTEL_DOTNET_TRACER_HOME: "{{ linuxProfilerPath }}/{{ commit_hash }}/tracer-home-linux"
          OTEL_INTEGRATIONS: "{{ linuxProfilerPath }}/{{ commit_hash }}/tracer-home-linux/integrations.json"
          OTEL_AGENT_HOST: "{{ controllerIp }}"
          OTEL_TRACE_LOGGING_RATE: 6
          OTEL_TRACE_DEBUG: 0
        options:
          requiredOperatingSystem: linux
          requiredArchitecture: x64
          buildFiles:
          - "../../tracer-home-linux/**;{{ linuxProfilerPath }}/{{ commit_hash }}/tracer-home-linux"
      load:
        endpoints:
          - http://localhost:5010

  linux_arm64:
    variables:
      serverAddress: "{{ linuxArm64Ip }}"
    jobs:
      application:
        endpoints:
          - "{{ linuxArm64Endpoint }}"
        environmentVariables:
          COR_PROFILER: "{918728DD-259F-4A6A-AC2B-B85E1B658318}"
          COR_PROFILER_PATH: "{{ linuxArm64ProfilerPath }}/{{ commit_hash }}/tracer-home-linux-arm64/OpenTelemetry.AutoInstrumentation.ClrProfiler.Native.so"
          CORECLR_PROFILER: "{918728DD-259F-4A6A-AC2B-B85E1B658318}"
          CORECLR_PROFILER_PATH: "{{ linuxArm64ProfilerPath }}/{{ commit_hash }}/tracer-home-linux-arm64/OpenTelemetry.AutoInstrumentation.ClrProfiler.Native.so"
          OTEL_DOTNET_TRACER_HOME: "{{ linuxArm64ProfilerPath }}/{{ commit_hash }}/tracer-home-linux-arm64"
          OTEL_INTEGRATIONS: "{{ linuxArm64ProfilerPath }}/{{ commit_hash }}/tracer-home-linux-arm64/integrations.json"
          OTEL_AGENT_HOST: "{{ controllerIp }}"
          OTEL_TRACE_LOGGING_RATE: 6
          OTEL_TRACE_DEBUG: 0
        options:
          requiredOperatingSystem: linux
          requiredArchitecture: arm64
          buildFiles:
          - "../../tracer-home-linux-arm64/**;{{ linuxArm64ProfilerPath }}/{{ commit_hash }}/tracer-home-linux-arm64"
      load:
        endpoints:
          - http://localhost:5010
