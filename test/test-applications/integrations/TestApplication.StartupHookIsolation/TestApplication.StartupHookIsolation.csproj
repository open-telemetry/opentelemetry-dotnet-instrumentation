<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- StartupHook isolation is only supported on .NET (Core), not .NET Framework -->
    <TargetFrameworks>net11.0;net10.0</TargetFrameworks>
  </PropertyGroup>
</Project>
